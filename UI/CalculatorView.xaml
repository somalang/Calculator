<Window x:Class="Calculator.UI.Views.CalculatorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Calculator.UI.ViewModels"
        Title="계산기" Height="500" Width="320"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        Background="White">

    <Window.DataContext>
        <vm:CalculatorViewModel />
    </Window.DataContext>

    <Window.Resources>
        <Style x:Key="BaseButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NumberButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="#F8F8F8"/>
            <Setter Property="Foreground" Value="Black"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E8E8E8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="OperatorButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="#F8F8F8"/>
            <Setter Property="Foreground" Value="Black"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E8E8E8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="FunctionButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Foreground" Value="Black"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E0E0E0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="EqualsButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#357ABD"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" Background="White" BorderBrush="#D0D0D0" BorderThickness="1" Margin="5">
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="70"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="8" Height="8" Fill="#D0D0D0" Margin="3,0"/>
                    <Ellipse Width="8" Height="8" Fill="#D0D0D0" Margin="3,0"/>
                    <Ellipse Width="8" Height="8" Fill="#D0D0D0" Margin="3,0"/>
                </StackPanel>

                <Border Grid.Column="1" Width="20" Height="20" 
                        BorderBrush="Black" BorderThickness="1" CornerRadius="10">
                    <Path Data="M10,2 L10,10 L15,15" Stroke="Black" StrokeThickness="1" 
                          Margin="2"/>
                </Border>
            </Grid>

            <Border Grid.Row="1" Background="#2C2C2C" 
                    CornerRadius="8" Margin="0,10,0,15">
                <TextBox Text="{Binding Display, Mode=OneWay}" 
                         FontSize="32" FontWeight="Normal" Foreground="White" Background="Transparent"
                         BorderThickness="0" IsReadOnly="True" TextAlignment="Right"
                         VerticalAlignment="Center" HorizontalAlignment="Stretch"
                         Padding="15,0"/>
            </Border>

            <UniformGrid Grid.Row="2" Rows="6" Columns="4">
                <Button Content="%" Command="{Binding PercentCommand}"
                        Style="{StaticResource FunctionButtonStyle}"/>
                <Button Content="CE" Command="{Binding ClearCommand}" 
                        Style="{StaticResource FunctionButtonStyle}"/>
                <Button Content="C" Command="{Binding ClearAllCommand}" 
                        Style="{StaticResource FunctionButtonStyle}"/>
                <Button Content="⌫" Command="{Binding BackspaceCommand}" 
                        Style="{StaticResource FunctionButtonStyle}"/>

                <Button Content="1/ₓ" Command="{Binding ReciprocalCommand}"
                        Style="{StaticResource FunctionButtonStyle}"/>
                <Button Content="x²" Command="{Binding SquareCommand}" 
                        Style="{StaticResource FunctionButtonStyle}"/>
                <Button Content="√" Command="{Binding SqrtCommand}" 
                        Style="{StaticResource FunctionButtonStyle}"/>
                <Button Content="/" Command="{Binding OperatorCommand}" 
                        CommandParameter="/" Style="{StaticResource OperatorButtonStyle}"/>

                <Button Content="7" Command="{Binding NumberCommand}" 
                        CommandParameter="7" Style="{StaticResource NumberButtonStyle}"/>
                <Button Content="8" Command="{Binding NumberCommand}" 
                        CommandParameter="8" Style="{StaticResource NumberButtonStyle}"/>
                <Button Content="9" Command="{Binding NumberCommand}" 
                        CommandParameter="9" Style="{StaticResource NumberButtonStyle}"/>
                <Button Content="*" Command="{Binding OperatorCommand}" 
                        CommandParameter="*" Style="{StaticResource OperatorButtonStyle}"/>

                <Button Content="4" Command="{Binding NumberCommand}" 
                        CommandParameter="4" Style="{StaticResource NumberButtonStyle}"/>
                <Button Content="5" Command="{Binding NumberCommand}" 
                        CommandParameter="5" Style="{StaticResource NumberButtonStyle}"/>
                <Button Content="6" Command="{Binding NumberCommand}" 
                        CommandParameter="6" Style="{StaticResource NumberButtonStyle}"/>
                <Button Content="-" Command="{Binding OperatorCommand}" 
                        CommandParameter="-" Style="{StaticResource OperatorButtonStyle}"/>

                <Button Content="1" Command="{Binding NumberCommand}" 
                        CommandParameter="1" Style="{StaticResource NumberButtonStyle}"/>
                <Button Content="2" Command="{Binding NumberCommand}" 
                        CommandParameter="2" Style="{StaticResource NumberButtonStyle}"/>
                <Button Content="3" Command="{Binding NumberCommand}" 
                        CommandParameter="3" Style="{StaticResource NumberButtonStyle}"/>
                <Button Content="+" Command="{Binding OperatorCommand}" 
                        CommandParameter="+" Style="{StaticResource OperatorButtonStyle}"/>

                <!-- +/- 커맨드 추가해야 함 -->
                <Button Content="+/-" Command="{Binding ToggleSignCommand}"
                        CommandParameter="+/-" Style="{StaticResource NumberButtonStyle}"/>
                <Button Content="0" Command="{Binding NumberCommand}" 
                        CommandParameter="0" Style="{StaticResource NumberButtonStyle}"/>
                <Button Content="." Command="{Binding NumberCommand}" 
                        CommandParameter="." Style="{StaticResource NumberButtonStyle}"/>
                <Button Content="=" Command="{Binding EqualsCommand}" 
                        Style="{StaticResource EqualsButtonStyle}"/>
            </UniformGrid>
        </Grid>
    </Border>
</Window>
